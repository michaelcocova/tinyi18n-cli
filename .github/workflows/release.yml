name: Release

on:
  workflow_dispatch:
  push:
    tags:
      # 只发布 tag
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          node-version: 24
          cache: true

      - name: Setup Node.js registry
        uses: actions/setup-node@v4
        with:
          registry-url: https://registry.npmjs.org

      # 安装依赖
      - name: Install
        run: vp install
      # 确保 tag 提交在 main 分支上，避免从其他分支打 tag 误发 npm。
      - name: Ensure tag commit is on main
        run: |
          git fetch origin main --depth=1
          git merge-base --is-ancestor "$GITHUB_SHA" origin/main

      # 检查代码
      - name: Check
        run: vp check
      # 测试代码
      - name: Test
        run: vp run -r test
      # 构建代码
      - name: Build
        run: vp run -r build
      # 发布到 npm
      - name: Publish to npm
        run: pnpm publish --access public --registry https://registry.npmjs.org/ --no-git-checks -r
        env:
          # NPM 令牌
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
